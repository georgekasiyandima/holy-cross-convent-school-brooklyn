# Minimal .vercelignore - only ignore what we must
# DO NOT ignore frontend/ directory

# Only root package.json
/package.json
/package-lock.json

# Only root markdown files
/*.md

# Backend directory
/backend/

# Root node_modules
/node_modules/

# That's it - everything else should be included, especially frontend/
